name: LLM Based ChatBot Actions

on:
  push:
    branches-ignore:
        - main

jobs:
    Do_nothing:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Run echo
              run:
                echo 'I m doing nothing'
